components:
  responses:
    Badrequest:
      description: "잘못된 요청"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/errorresponse"

    Unauthorized:
      description: "인증 실패"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/errorresponse"
