components:
  schemas:
    Account:
      type: object
      properties:
        currency:
          type: string
          description: "화폐를 의미하는 영문 대문자 코드"
        balance:
          type: string
          description: "주문 가능 금액/수량"
        locked:
          type: string
          description: "주문 중 묶여있는 금액/수량"
        avg_buy_price:
          type: string
          description: "매수평균가"
        avg_buy_price_modified:
          type: boolean
          description: "매수평균가 수정 여부"
        unit_currency:
          type: string
          description: "평단가 기준 화폐"
